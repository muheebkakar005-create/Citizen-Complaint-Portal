# ==========================================
# CITIZEN COMPLAINT PORTAL - BACKEND ENV VARS
# Generated for local development. NEVER commit this file to version control.
# ==========================================

# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string
MONGODB_URI="mongodb+srv://izzatkakar703_db_user:WfBVjhr0nSfdaKTp@cluster0.lmwrcmw.mongodb.net/?appName=Cluster0"
# JWT (randomly generated - safe to keep as-is, or regenerate your own)
JWT_SECRET="a8e9e6b64a9e6b92627f6b9de1f051acb45136b760dd1490655cc6b8f57799e156a6317f5a4df089bf334c9e0893730e"
JWT_EXPIRES_IN=7d

# Frontend URL(s) allowed by CORS - comma-separate if you need more than one
CLIENT_URL=http://localhost:5173,http://localhost:5174

# Google Gemini API (optional - used only on the backend for the officer AI
# briefing; the app works fine without it via a local fallback summary)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Cloudinary (optional - image upload). App works fine without these; the
# frontend already sends images as inline base64 data URLs by default.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

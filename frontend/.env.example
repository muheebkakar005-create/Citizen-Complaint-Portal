# Optional. Only needed if your backend isn't running on the default
# http://localhost:5000. Used by vite.config.ts to configure the dev-server
# proxy for /api/* requests.
VITE_BACKEND_URL=http://localhost:5000
